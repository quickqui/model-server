functions:
  - name: NotDone
    menuPath: Issue
    icon: done
    base:
      resource: Issue
      crud: list
    filter:
      done: false
    sort:
      createTime: DESC
    actions:
      - FinishIssue
